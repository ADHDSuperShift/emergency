{
  "framework": "vite",
  "installCommand": "npm ci --no-optional",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
